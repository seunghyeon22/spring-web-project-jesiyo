<nav aria-label="Page navigation example">
    <ul class="pagination justify-content-center"> {{#pagination.pages}}
        <li class="page-item {{#isActive}}active{{/isActive}}">
            <a class="page-link" href="#" onclick="changePage({{pageNumber}})">{{pageNumber}}</a>
        </li> {{/pagination.pages}}
    </ul>
</nav>

