<!DOCTYPE html>
<html lang="en">
<head>
    <title>Header</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" type="text/css" href="/css/nav.css">
    <link rel="stylesheet" type="text/css" href="/css/header.css">

    <link rel="stylesheet" href="/css/myPage.css">

    <style>
        @font-face {
            font-family: 'contentFont'; /* 폰트 이름 지정 */
            src: url('/fonts/BMDOHYEON.ttf') format('truetype'); /* 경로와 파일 형식 */
        }

        .card-body {
            font-family: 'contentFont', serif;
        }

        .title-box {
            font-family: 'contentFont', serif;
        }
    </style>
</head>
<body>


{{>layout/header}}
<div class="container d-flex mt-5">
    {{>layout/mypage-menu}}
    <div class="box">
        <div class="title-box">
            <h2>경매중인 물품</h2>
        </div>
        <div class="item-list mt-5">
            {{#models}}
                <div class="card mb-3">
                    <div class="card-image">
                        <img src="{{imgUrl}}" class="card-img-top"/>
                    </div>
                    <div class="card-body mt-2">
                        <h4 class="card-title">{{title}}</h4>
                        <div class="card-text mt-2">
                            <input type="hidden" name="id" id="{{id}}">
                            <div>카테고리 : {{category}}</div>
                            <div>경매 종료 일시 : {{endAt}}</div>
                            <div>시작 입찰가 : {{startingPrice}}원</div>
                            <div>현재 입찰가 : {{tryPrice}}</div>
                            <div class="btn-group mt-1">
                                <button type="button" class="btn btn-custom btn-sm" onclick="deleteGoods({{id}})">경매
                                    취소
                                </button>
                                <button type="button" class="btn btn-custom btn-sm" onclick="insertTransaction({{id}})">
                                    조기 종료
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            {{/models}}
            {{>layout/paging}}
        </div>
    </div>
</div>
{{>layout/footer}}

<script type="text/javascript" src="/js/being-auctioned.js"></script>
</body>
<script>
    function changePage(pageNumber) {
        window.location.href = `/s/mypage-being-auctioned?page=${pageNumber}`;
    }
</script>
</html>
